{% comment %} 
                ============================================================================
                WARRANTY SECTION 
                - Two-column layout: image left, content right 
                - Fully responsive: stacks on mobile 
                - Customizable via Theme Customizer 
                - Tailwind CSS styling
                ============================================================================ 
{% endcomment %}

<section class="py-12 md:py-20">
  <div class="container mx-auto px-4 grid grid-cols-1 md:grid-cols-2 gap-8 md:gap-12 items-center">

    {%- if section.settings.image != blank -%}
      <div>
        <img
          height=""
          width=""
          src="{{ section.settings.image | image_url: width: 800 }}"
          alt="{{ section.settings.image_alt | escape }}"
          loading="lazy"
          class="w-full h-auto rounded shadow-lg object-cover">
      </div>
    {%- endif -%}

    <div class="text-center md:text-left">
      {%- if section.settings.kicker != blank -%}
        <p class="text-lg text-emerald-800 tracking-wide uppercase mb-2">
          {{ section.settings.kicker }}
        </p>
      {%- endif -%}

      {%- if section.settings.heading != blank -%}
        <h2 class="text-3xl md:text-4xl font-bold text-emerald-900 mb-4">
          {{ section.settings.heading }}
        </h2>
      {%- endif -%}

      {%- if section.settings.text != blank -%}
        <div class="text-gray-700 leading-relaxed mb-4">
          {{ section.settings.text }}
        </div>
      {%- endif -%}

      {%- if section.settings.note != blank -%}
        <p class="italic text-sm text-gray-800 mb-6">
          {{ section.settings.note }}
        </p>
      {%- endif -%}

      {%- if section.settings.button_label != blank and section.settings.button_link != blank -%}
        <a href="{{ section.settings.button_link }}" class="inline-block bg-emerald-700 hover:bg-emerald-800 text-white font-medium px-6 py-3 rounded transition">
          {{ section.settings.button_label }}
        </a>
      {%- endif -%}
    </div>

  </div>
</section>

{% schema %}
  {
    "name": "Warranty Section",
    "settings": [
      {
        "type": "image_picker",
        "id": "image",
        "label": "Left Image"
      },
      {
        "type": "text",
        "id": "image_alt",
        "label": "Image Alt Text",
        "default": "Warranty section image"
      },
      {
        "type": "text",
        "id": "kicker",
        "label": "Kicker Text",
        "default": "OUR EXCLUSIVE"
      },
      {
        "type": "text",
        "id": "heading",
        "label": "Heading",
        "default": "WARRANTY"
      }, {
        "type": "richtext",
        "id": "text",
        "label": "Body Text",
        "default": "<p>We are thankful that our patients choose to come to us for eyewear. We care about satisfaction so offer a warranty on our prescriptions and glasses. If you are not happy with frames or lenses, bring them back within 90 days so we can remake them. If we cannot find eyewear to improve vision, we offer a partial refund within 90 days of purchase date. We also offer a one year scratch warranty on our lenses.</p>"
      }, {
        "type": "text",
        "id": "note",
        "label": "Note (Italic)",
        "default": "*Some exclusions apply"
      }, {
        "type": "text",
        "id": "button_label",
        "label": "Button Label",
        "default": "Learn More"
      }, {
        "type": "url",
        "id": "button_link",
        "label": "Button Link"
      }
    ],
    "presets": [
      {
        "name": "Warranty Section",
        "category": "Custom Sections"
      }
    ]
  }
{% endschema %}